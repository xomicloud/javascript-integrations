"use strict";

const { API_HOST, CLIENT_ID, CLIENT_SECRET } = process.env;

const apiHost = API_HOST, ///
      clientId = CLIENT_ID, ///
      clientSecret = CLIENT_SECRET ///

module.exports = {
  apiHost,
  clientId,
  clientSecret
};
